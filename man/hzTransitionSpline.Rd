% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hzTransitionSpline.R
\name{hzTransitionSpline,SoilProfileCollection-method}
\alias{hzTransitionSpline,SoilProfileCollection-method}
\alias{hzTransitionSpline}
\title{Mass-preserving Spline with Transition Zones}
\usage{
\S4method{hzTransitionSpline}{SoilProfileCollection}(
  object,
  var,
  hzbr = NULL,
  hzbr_nm = NULL,
  d = c(0, 5, 15, 30, 60, 100, 200),
  method = c("est_1cm", "est_icm", "est_dcm"),
  type = c("quadratic", "linear"),
  vlow = 0,
  vhigh = Inf,
  min_thick = 2,
  max_thick = 15,
  FUN = hzTransitionSplineSolve,
  entropyFUN = shannonEntropy,
  ...
)
}
\arguments{
\item{object}{A SoilProfileCollection}

\item{var}{The name of the horizon-level variable(s) to spline.}

\item{hzbr}{Column name containing horizon boundary
distinctness codes (e.g., 'V', 'A', 'C', 'G', 'D') or numeric thickness in cm.
Alternatively, a transition probability (TP) matrix (e.g. from \code{hzTransitionProbabilities()}).
Default \code{NULL} uses a default thickness of 1cm for all boundaries.}

\item{hzbr_nm}{Column name used for lookup when \code{hzbr} is a transition
probability matrix. Defaults to \code{GHL(object)}.}

\item{d}{Output depth intervals (cm). Default \code{c(0, 5, 15, 30, 60, 100, 200)}.}

\item{method}{Return method:
\itemize{
\item 'est_1cm' (default) returns 1cm slices
\item 'est_icm' returns original input horizon boundaries with splined values
\item 'est_dcm' returns averages over specified depth intervals \code{d}
}}

\item{type}{Type of transition:
\itemize{
\item 'quadratic' (smooth, \eqn{ C^1 } continuous at boundaries and horizon centers)
\item 'linear' (linear gradient in transition zone)
}}

\item{vlow}{Minimum allowable value. Default: \code{0}.}

\item{vhigh}{Maximum allowable value. Default: \code{Inf}.}

\item{min_thick}{Minimum transition thickness (cm) when using a TP matrix. Default: \code{2}.}

\item{max_thick}{Maximum transition thickness (cm) when using a TP matrix. Default: \code{15}.}

\item{FUN}{A solver function. Default: \code{hzTransitionSplineSolve}.}

\item{entropyFUN}{An entropy function for TP matrix mapping. Default: \code{shannonEntropy}.}

\item{...}{Additional arguments (not used)}
}
\value{
A SoilProfileCollection
}
\description{
This function implements a mass-preserving spline that specifically targets
transition zones between horizons, while keeping the central parts of
horizons at a constant value. The transition zone thickness can be
parameterized using distinctness classes or specified directly.
}
\details{
The tridiagonal system is solved to find constant values \eqn{V_i} for each
horizon \code{i} such that the total mass is preserved, accounting for the
transitions between horizons.

For 'quadratic' transitions, the function is constant in the middle of
the horizon and joins the neighbor horizon with a quadratic function
in the transition zone of half-width delta.

For 'linear' transitions, the function is constant in the middle and
has a linear gradient in the transition zone \eqn{[z_b - \delta, z_b + \delta]}.

When \code{hzbr} is a transition probability matrix, transition widths are assigned
based on the probability of transition between adjacent horizons and the
Shannon Entropy of the source horizon. Shorter (sharper) widths are assigned
to high-probability transitions from low-entropy states. Larger (more diffuse)
widths are assigned to low-probability transitions or transitions from
high-entropy states (indicating confusion).

Width \eqn{2 \cdot \delta} is calculated as:
\eqn{w = min\_thick + (max\_thick - min\_thick) \cdot (1 - P_{ij} \cdot (1 - H_{norm}))}
where \eqn{P_{ij}} is the transition probability and \eqn{H_{norm}} is the
normalized Shannon Entropy of the source horizon's transitions.
}
\examples{
data(sp1)
depths(sp1) <- id ~ top + bottom

# basic usage
res <- hzTransitionSpline(sp1, "prop")

# plot result
plotSPC(
  res[1:5, ],
  color = "prop_spline",
  col.palette = rev(hcl.colors(10)),
  divide.hz = FALSE
)

# use distinctness classes (abrupt, clear, gradual) in horizons(sp1)$bound_distinct
res2 <- hzTransitionSpline(sp1, "prop", hzbr = "bound_distinct")
plotSPC(
  res2[1:5, ],
  color = "prop_spline",
  col.palette = rev(hcl.colors(10)),
  divide.hz = FALSE
)

# linear transitions
res3 <- hzTransitionSpline(sp1, "prop", type = "linear")
plotSPC(
  res3[1:5, ],
  color = "prop_spline",
  col.palette = rev(hcl.colors(10)),
  divide.hz = FALSE
)

# use transition probability matrix
data(sp4)
depths(sp4) <- id ~ top + bottom
hzdesgnname(sp4) <- "name"
sp4 <- generalizeHz(sp4, pattern = c("^A","^B"), new = c("A", "Bt"))
tp <- hzTransitionProbabilities(sp4, "genhz")
res4 <- hzTransitionSpline(sp4, "clay", hzbr = tp, hzbr_nm = "genhz")
plotSPC(res4, color = "clay_spline", divide.hz = FALSE)

}
\seealso{
\code{\link[=hzTransitionProbabilities]{hzTransitionProbabilities()}} \code{\link[=shannonEntropy]{shannonEntropy()}}
}
\author{
Andrew G. Brown
}
